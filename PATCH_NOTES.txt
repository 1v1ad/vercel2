GGRoom vercel2 patch: user-card Accounts tab improvements

Changed:
- admin/user-card.js
  * 'Связи по устройству' rendering: now shows two pills inside each device group:
    - device_id + list of user_ids seen on that device
    - separate pill for the first shadow (non-current) user_id (+N if more)
  * Shadow pill is clickable (opens user-card for that user_id).

Deploy:
- Replace admin/user-card.js in frontend repo and redeploy.
